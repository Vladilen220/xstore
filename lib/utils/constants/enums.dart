// LIST of ENUMS
//They cannot be created inside a class >reminder<

enum ProductType {single ,variable}

enum TextSizes { small, medium, large}

enum OrderStatus { processing, shipped, delivered,pending}

enum PaymentsMethods { paypal, googlePlay , applePay, visa, masterCard, creditCard, payStack, razorPay , paytm}